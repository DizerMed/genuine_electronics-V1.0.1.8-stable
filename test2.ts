export function test() {
  const arr = [1, 2, 3];
  try {
    for (const a of arr) {
      try {
        if (a === 1) {
        } else if (a === 2) {
        } else {
        }
      } catch (e) {
      }
    }
    if (arr.length === 0) {
    } else {
    }
  } finally {
  }
}
